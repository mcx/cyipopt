version: 2
build:
  os: "ubuntu-24.04"
  tools:
    python: "miniconda-latest"
sphinx:
  configuration: docs/source/conf.py
formats: all
conda:
  environment: docs/rtd-env.yml
python:
  install:
    - method: setuptools
      path: .
